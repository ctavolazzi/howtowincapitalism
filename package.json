{
  "name": "howtowincapitalism",
  "version": "1.0.0",
  "type": "module",
  "description": "A satirical but practical wiki about financial autonomy",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro build && echo '✓ Build passed'",
    "deploy": "wrangler pages deploy dist",
    "ship": "node scripts/ship.mjs",
    "og-image": "node scripts/generate-og-image.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "new": "node scripts/new-page.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:css": "stylelint 'src/**/*.{css,astro}' --allow-empty-input",
    "lint:css:fix": "stylelint 'src/**/*.{css,astro}' --fix --allow-empty-input",
    "a11y:colors": "echo '🎨 Color Accessibility Check' && grep -rn '#[0-9a-fA-F]\\{3,6\\}' src/components --include='*.astro' --include='*.css' | grep -v 'var(--' | grep 'color' && echo '❌ Found hardcoded colors!' || echo '✅ No hardcoded colors found'"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@astrojs/mdx": "^4.3.12",
    "@astrojs/sitemap": "^3.6.0",
    "@astrojs/starlight": "^0.37.0",
    "@nanostores/persistent": "^1.2.0",
    "astro": "^5.6.1",
    "nanostores": "^1.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.15",
    "@vitest/ui": "^4.0.15",
    "sharp": "^0.34.5",
    "stylelint": "^16.26.1",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-declaration-strict-value": "^1.10.11",
    "vitest": "^4.0.15",
    "wrangler": "^4.53.0"
  }
}
