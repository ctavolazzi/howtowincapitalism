---
/**
 * @fileoverview Email Confirmation Error Page
 *
 * Error page displayed when email confirmation fails. Shows
 * contextual error messages based on the reason query parameter.
 *
 * @module pages/confirm/error/index
 * @see {@link module:pages/api/auth/confirm} - Redirects here on error
 * @see {@link module:pages/confirm/success/index} - Success counterpart
 *
 * ## Route
 *
 * `/confirm/error/?reason=...` - Public
 *
 * ## Query Parameters
 *
 * | Param | Description |
 * |-------|-------------|
 * | reason | Error code for contextual message |
 *
 * ## Error Codes
 *
 * | Code | Message |
 * |------|---------|
 * | missing-token | No confirmation token was provided |
 * | invalid-token | Link is invalid or already used |
 * | expired-token | Link has expired (24hr limit) |
 * | service-unavailable | Service temporarily down |
 * | server-error | Unexpected error occurred |
 *
 * ## UI Elements
 *
 * - Error icon (X mark)
 * - "Confirmation Failed" heading
 * - Dynamic error message from reason param
 * - Actions: "Register Again" + "Try Login"
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirmation Failed | How To Win Capitalism</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <style>
    :root {
      --color-bg: #ffffff;
      --color-text: #202122;
      --color-text-muted: #72777d;
      --color-primary: #0645ad;
      --color-border: #a2a9b1;
      --color-error: #c00;
      --color-error-bg: #fee;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--color-bg);
      color: var(--color-text);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      padding: 1rem 2rem;
      border-bottom: 1px solid var(--color-border);
    }

    .site-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--color-text);
      text-decoration: none;
    }

    .site-title:hover {
      color: var(--color-primary);
    }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      text-align: center;
    }

    .error-container {
      max-width: 500px;
      padding: 3rem;
      background: var(--color-error-bg);
      border: 1px solid var(--color-error);
      border-radius: 8px;
    }

    .error-icon {
      font-size: 4rem;
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 1.75rem;
      margin-bottom: 1rem;
      color: var(--color-error);
    }

    p {
      margin-bottom: 1rem;
      color: var(--color-text);
      line-height: 1.6;
    }

    .reason {
      font-size: 0.9rem;
      color: var(--color-text-muted);
      margin-bottom: 1.5rem;
    }

    .actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .button {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      text-decoration: none;
      border-radius: 4px;
      font-weight: 600;
      transition: opacity 0.2s;
    }

    .button-primary {
      background: var(--color-primary);
      color: #ffffff;
    }

    .button-secondary {
      background: transparent;
      color: var(--color-primary);
      border: 1px solid var(--color-primary);
    }

    .button:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <header>
    <a href="/" class="site-title">How To Win Capitalism</a>
  </header>

  <main>
    <div class="error-container">
      <div class="error-icon">✕</div>
      <h1>Confirmation Failed</h1>
      <p>
        We couldn't verify your email address.
        The confirmation link may be invalid or expired.
      </p>
      <p class="reason" id="reason"></p>
      <div class="actions">
        <a href="/register/" class="button button-primary">Register Again</a>
        <a href="/login/" class="button button-secondary">Try Login</a>
      </div>
    </div>
  </main>

  <script>
    // Show reason if provided
    const params = new URLSearchParams(window.location.search);
    const reason = params.get('reason');
    const reasonEl = document.getElementById('reason');

    if (reason && reasonEl) {
      const messages: Record<string, string> = {
        'missing-token': 'No confirmation token was provided.',
        'invalid-token': 'The confirmation link is invalid or has already been used.',
        'expired-token': 'The confirmation link has expired (links expire after 24 hours).',
        'service-unavailable': 'Service temporarily unavailable. Please try again later.',
        'server-error': 'An unexpected error occurred. Please try again.',
      };
      reasonEl.textContent = messages[reason] || `Error: ${reason}`;
    }
  </script>
</body>
</html>
