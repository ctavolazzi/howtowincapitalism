---
/**
 * @fileoverview Reset Password Page
 *
 * Password reset page that accepts a token from the URL and allows
 * users to set a new password. Form validates token before showing.
 *
 * @module pages/reset-password/index
 * @see {@link module:components/auth/ResetPasswordForm} - Form component
 * @see {@link module:pages/api/auth/reset-password} - Reset API
 * @see {@link module:pages/forgot-password/index} - Token request page
 *
 * ## Route
 *
 * `/reset-password/?token=...` - Public, requires valid token
 *
 * ## URL Parameters
 *
 * | Param | Required | Description |
 * |-------|----------|-------------|
 * | token | Yes | 64-char hex reset token from email |
 *
 * ## Page States
 *
 * Handled by ResetPasswordForm component:
 * 1. Loading - Validating token
 * 2. Invalid - Token expired or not found
 * 3. Form - Token valid, enter new password
 * 4. Success - Password changed, link to login
 *
 * ## User Flow
 *
 * ```
 * 1. User clicks link in reset email
 * 2. Page loads with token in URL
 * 3. Token validated via API
 * 4. User enters and confirms new password
 * 5. Redirected to login on success
 * ```
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */
import ResetPasswordForm from '../../components/auth/ResetPasswordForm.astro';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Password | How To Win Capitalism</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <style>
    :root {
      --color-bg: #ffffff;
      --color-text: #202122;
      --color-text-muted: #72777d;
      --color-primary: #0645ad;
      --color-border: #a2a9b1;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--color-bg);
      color: var(--color-text);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      padding: 1rem 2rem;
      border-bottom: 1px solid var(--color-border);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .site-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--color-text);
      text-decoration: none;
    }

    .site-title:hover {
      color: var(--color-primary);
    }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 2rem;
    }

    .page-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .page-header h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .page-header p {
      color: var(--color-text-muted);
    }
  </style>
</head>
<body>
  <header>
    <a href="/" class="site-title">How To Win Capitalism</a>
  </header>

  <main>
    <div class="page-header">
      <h1>Reset Password</h1>
      <p>Create a new password for your account</p>
    </div>

    <ResetPasswordForm />
  </main>
</body>
</html>
