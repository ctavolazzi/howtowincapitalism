---
/**
 * @fileoverview Profile redirect page.
 *
 * Utility route that redirects users to their profile:
 * - Authenticated users: Redirect to /users/{userId}/
 * - Not logged in: Redirect to /login with redirect param
 *
 * @module pages/profile
 * @see {@link module:pages/users/[id]} - User profile destination
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting... | How To Win Capitalism</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      background: #f5f5f5;
    }
    .loading {
      text-align: center;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="loading">
    <p>Redirecting to your profile...</p>
  </div>

  <script>
    import { checkAuth } from '../../lib/auth/api-client';

    async function redirect() {
      const { authenticated, user } = await checkAuth();

      if (authenticated && user) {
      // Redirect to the user's editable profile
      window.location.href = `/users/${user.id}/`;
    } else {
      // Not logged in, go to login
        window.location.href = '/login/?redirect=/profile/';
      }
    }

    redirect();
  </script>
</body>
</html>
