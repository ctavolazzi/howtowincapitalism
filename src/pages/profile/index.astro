---
/**
 * @fileoverview Profile Index Redirect
 *
 * Convenience route that redirects users to their own profile page.
 * Unauthenticated users are redirected to login with return URL.
 *
 * @module pages/profile/index
 * @see {@link module:pages/users/[id]} - Profile destination
 * @see {@link module:pages/login/index} - Login redirect
 * @see {@link module:lib/auth/api-client} - Auth checking
 *
 * ## Route
 *
 * `/profile/` - Protected (requires authentication)
 *
 * ## Redirect Logic
 *
 * | Auth State | Destination |
 * |------------|-------------|
 * | Logged in | `/users/{userId}/` |
 * | Not logged in | `/login/?redirect=/profile/` |
 *
 * ## Usage
 *
 * ```html
 * <!-- Link to "my profile" without knowing user ID -->
 * <a href="/profile/">My Profile</a>
 * ```
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting... | How To Win Capitalism</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      background: #f5f5f5;
    }
    .loading {
      text-align: center;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="loading">
    <p>Redirecting to your profile...</p>
  </div>

  <script>
    import { checkAuth } from '../../lib/auth/api-client';

    async function redirect() {
      const { authenticated, user } = await checkAuth();

      if (authenticated && user) {
      // Redirect to the user's editable profile
      window.location.href = `/users/${user.id}/`;
    } else {
      // Not logged in, go to login
        window.location.href = '/login/?redirect=/profile/';
      }
    }

    redirect();
  </script>
</body>
</html>
