---
/**
 * @fileoverview Tabs container using details/summary for no-JS support.
 *
 * Simple tabbed content container. Uses native details/summary
 * elements for accessibility and no-JavaScript fallback.
 *
 * @module components/simple/Tabs
 * @see {@link module:components/simple/TabItem} - Child tab item
 *
 * @example
 * ```astro
 * <Tabs>
 *   <TabItem label="Tab 1">Content 1</TabItem>
 *   <TabItem label="Tab 2">Content 2</TabItem>
 * </Tabs>
 * ```
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */
---

<div class="tabs">
  <slot />
</div>

<style>
  .tabs {
    border: 1px solid #ccc;
    border-radius: 4px;
    margin: 1rem 0;
  }

  .tabs :global(details) {
    border-bottom: 1px solid #ccc;
  }

  .tabs :global(details:last-child) {
    border-bottom: none;
  }

  .tabs :global(summary) {
    padding: 0.75rem 1rem;
    cursor: pointer;
    background: #f8f9fa;
    font-weight: 500;
  }

  .tabs :global(summary:hover) {
    background: #eee;
  }

  .tabs :global(details > div),
  .tabs :global(details > p) {
    padding: 1rem;
  }
</style>
