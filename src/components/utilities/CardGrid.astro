---
/**
 * @fileoverview CardGrid utility for responsive card layouts.
 *
 * A responsive grid container for TopicCard components.
 * Handles layout so cards don't need to know about grid.
 *
 * ## Column Options
 * - `2`: Two-column layout (default)
 * - `3`: Three-column layout
 * - `4`: Four-column layout
 *
 * Auto-adjusts to single column on mobile.
 *
 * @module components/utilities/CardGrid
 * @see {@link module:components/molecules/TopicCard} - Child component
 *
 * @example
 * ```astro
 * <CardGrid>
 *   <TopicCard ... />
 *   <TopicCard ... />
 * </CardGrid>
 *
 * <CardGrid columns={3} gap="lg">
 *   ...
 * </CardGrid>
 * ```
 *
 * @author How To Win Capitalism Team
 * @since 1.0.0
 */

export interface Props {
  /** Number of columns (default: 2, auto-adjusts on mobile) */
  columns?: 2 | 3 | 4;
  /** Gap size */
  gap?: 'sm' | 'md' | 'lg';
  /** Optional CSS class */
  class?: string;
}

const {
  columns = 2,
  gap = 'md',
  class: className
} = Astro.props;

const gridClasses = [
  'card-grid',
  `card-grid--cols-${columns}`,
  `card-grid--gap-${gap}`,
  className
].filter(Boolean).join(' ');
---

<div class={gridClasses}>
  <slot />
</div>

<!-- Styles in Base.astro -->
